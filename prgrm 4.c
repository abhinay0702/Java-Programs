#include <stdioi.h>
int main()
{
    int a;
    int fac=1;
    printf("Enter a no. whose factorial is to be calculated");
    scanf("%d",&a);
    for(int i=0;i<=a;i++)
    {
        fac=fac*1;

    }
printf("FactoriaL is %d",fac);
}